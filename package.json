{
  "name": "z-stack-monorepo",
  "private": true,
  "scripts": {
    "build": "pnpm -r --filter=!demo build",
    "test": "pnpm -r --filter=!demo test",
    "dev": "pnpm --filter demo dev"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": ">=18"
  }
}
